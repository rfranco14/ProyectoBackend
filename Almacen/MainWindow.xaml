<Controls:MetroWindow  x:Class="Almacen.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:Almacen"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <Button Content="CATEGORIAS" Command="{Binding Instancia}" CommandParameter="Categorias"/>
            <Button Content="TIPO EMPAQUE" Command="{Binding Instancia}" CommandParameter="TipoEmpaques"/>
            <Button Content="PRODUCTO" Command="{Binding Instancia}" CommandParameter="Productos"/>

            <Button>
                <StackPanel Orientation="Horizontal">

                    <TextBlock Margin="4 0 0 0" VerticalAlignment="Center" Text="LOGIN" />
                </StackPanel>
            </Button>
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>


        <Grid>
        
    </Grid>
 </Controls:MetroWindow>
